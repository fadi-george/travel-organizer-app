{
  "name": "travel-organizer-app",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "setup:client": "./scripts/setup-client-env.sh",
    "dev:client": "cd client && flutter run",
    "dev:sql": "bun --hot server/sql/index.ts",
    "dev:convex": "bunx convex dev",
    "db:sql:generate": "bunx drizzle-kit generate",
    "db:sql:migrate": "bunx drizzle-kit migrate",
    "db:sql:push": "bunx drizzle-kit push",
    "db:sql:studio": "bunx drizzle-kit studio"
  },
  "dependencies": {
    "convex": "^1.31.2",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.11.3"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.8",
    "postgres": "^3.4.5"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
